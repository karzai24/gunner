# Humanoid animation contract

## Canonical skeleton and scope

Epic UE5 Manny/Quinn-compatible `SK_Mannequin` remains the provisional gameplay skeleton. `SKM_Manny_Simple` is the current visual. Custom Micah/Rook appearance waits until movement and weapon interaction are proven. The original foundation BP_Warden and installed ABP_Unarmed remain available; the committed BP_WardenMotion uses the project `ABP_WardenMotionPolish` graph, with an optional local profile selecting `ABP_WardenCoverMovement` at spawn; `ABP_WardenCrouchReload` and the earlier `ABP_WardenBlindFire` and `ABP_WardenMotion` graphs remain preserved.

The portable composition reuses Epic template motion and Quaternius CC0 source. The optional local profile also uses compatible installed Epic plugin animation copies and official Mixamo downloads, kept outside public Git content. No manually keyed replacement motions were authored. Imported or retargeted content is not automatically an accepted gameplay animation: live poses, transitions and deformation must be recorded in `TEST_MATRIX.md`.

## Source and retarget setup

- Unreal centimeters, Z up, actor forward +X; skeletal component yaw -90 degrees and standing mesh Z -90 cm. Preserve canonical scale and capsule dimensions.
- Installed Epic rifle/pistol animations already target the canonical skeleton. The project builds separate eight-direction standing walk/jog Blend Spaces, with source samples at 180 and 450 cm/s, plus their armed idle.
- The acquired Quaternius Universal Animation Library Standard v3 contains 43 clips. Its original in-place GLB and license are retained under `ArtSource/Quaternius`; imported source packages are separate under `Content/Gunner/Animation/Source/Quaternius`.
- `IK_Quaternius`, `IK_Manny` and `RTG_Quaternius_Manny` live under `Content/Gunner/Animation/Rigs`. The authoring script applies Unreal humanoid chain definitions and full-body IK, maps shared chains, aligns the target pose chain-to-chain and applies target foot grounding. Reference bone samples, chain maps, durations and output samples are written to a local retarget report.
- The seven generated Manny candidates are `A_UAL_Crouch_Idle_Loop`, `A_UAL_Crouch_Fwd_Loop`, `A_UAL_Sprint_Loop`, `A_UAL_Punch_Jab`, `A_UAL_Punch_Cross`, `A_UAL_Roll` and `A_UAL_Sword_Attack`. Source/target duration and skeleton binding are checked during authoring. Only actual live verification can accept foot contact, shoulders, hands or transitions.
- All current retarget outputs disable animation-track root motion and force root lock. CharacterMovement owns displacement; the dodge's native movement source is described below. No root-motion vault/warping action is enabled.

## Composed graph and action ownership

`UGunnerAnimInstance` snapshots character velocity, actor-relative forward/right speeds, grounded/stance state, aim pitch, sprint, cover and selected weapon. The graph consumes this state; it does not invent combat state.

The portable graph selects rifle/pistol standing locomotion, real crouch idle/forward motion, a distinct sprint clip and armed airborne loops. Its crouch uses speed magnitude and faces travel; crouched ADS remains stationary. The local graph enables genuine signed forward/backward/left/right crouch in free movement and high cover, while preserving a separate low-cover protective branch. `bDirectionalCrouchReady` in settings and `bDirectionalCrouchPoseReady` in the graph gate this extension.

Epic rifle/pistol Aim Offsets and an `UpperBody` montage slot blend from `spine_01` over the lower-body pose. The upper layer fades out during sprint, airborne movement and non-ADS crouch. Keeping the imported crouch torso intact avoids raising its head with the upright armed overlay; standing and crouched ADS restore the armed layer. This changes blend weight, not source animation tracks. A separate `FullBody` slot layers the jab/cross actions over locomotion; gameplay prevents movement and incompatible actions during it. The project registers `UpperBody` in the Weapon slot group and `FullBody` in the Traversal group on the canonical skeleton. That metadata change does not reauthor Epic animation tracks.

Fire, reload and equip montages use the installed Epic handling clips. Melee alternates the retargeted `Punch_Jab` and `Punch_Cross` montages. `AM_DodgeRoll` uses the genuine retargeted `Roll` in the FullBody slot at rate scale 1.65. A guarded native CharacterMovement MoveTo root-motion source supplies up to 350 cm of displacement over the montage duration; the acquired skeletal animation remains in-place. The standing capsule, route floor checks, action guards and interruption cleanup are owned by `UGunnerDodgeComponent`. Sword attack remains a dormant candidate. No sword/knife mechanic is enabled. The portable graph adds arm/hand IK during low-cover blind fire. The local graph additionally corrects the rifle support-hand grip during sprint, high-wall poses and established free/high-cover crouch carry. There is no runtime foot IK; foot contact and blending still require visual inspection.

Reload uses the protective arm composition described below during crouch and low-cover attachment. Native guards require its readiness flag so a previous graph cannot award ammunition for an invisible action. Equip and dry fire use the protective handling layer described below and require their own readiness flags in actual/pending crouch or low cover. Crouch input rejects active reload/equip. Standing high-cover reload remains available.

Current gun visuals attach to `hand_r` with per-weapon authored grip transforms derived from the actual armed idle. Muzzle offsets are stored in each Weapon Data Asset. Dedicated `weapon_r`, `weapon_back`, `muzzle` and `grip_l` sockets remain future authoring work. Static weapon models do not provide independently animated magazines or bolts.

Gameplay is authoritative. Reload transfers ammunition on a validated, uninterrupted montage-ended callback; stale callbacks are rejected with an action serial and timeouts clear locks without committing ammo. Each melee variant's timed impact request additionally checks the current montage/state and performs guarded geometry queries. Future notifies may request commits but must never own ammo/damage directly. Animation-track root-motion vault/warping remains disabled until its montage, collision path, destination, cancellation and later replication are proven. The current roll uses the bounded native movement source described above.

## Procedural low-cover blind fire

`ABP_WardenBlindFire` reuses the real Quaternius crouch and Epic armed source clips. A separate arm-only layer begins at `clavicle_l` and `clavicle_r`; root, pelvis, spine, neck and head retain the crouch pose. Native two-bone IK raises both hands without stretching, with hand rotations derived from one rigid weapon transform and the existing right-hand attachment. Each weapon's left grip is sampled from its own Epic ADS idle by `Tools/install_blind_fire.py`, not estimated from a generic gun.

The existing mesh-space additive fire montages evaluate after the IK pose so recoil remains visible; the final arm mask prevents the upright torso from replacing protected crouch. `BlindFireAlpha` blends the arm presentation while native combat waits for the actual evaluated hands/muzzle/head, valid cover and clearance. `bBlindFirePoseReady` and per-weapon `bBlindFireGripReady` keep old/unconfigured compositions disabled. The installer creates a new graph, refuses overwrites and unexpected previous classes, and backs up the assigned character and weapon assets before changes. No Epic or Quaternius animation tracks are modified or new keyframes authored. Live reach, protection and firing acceptance belongs in TEST_MATRIX.

## Coverage ledger

| Requirement | Current source/composition | Remaining acceptance / limitation |
|---|---|---|
| Standing armed idle/walk/jog | Epic rifle/pistol eight-direction clips and project Blend Spaces | Inspect both weapons, all directions and transitions in real gameplay |
| Airborne | Epic takeoff, fall loops and additive landing recovery | Project-owned copies lock root; CharacterMovement owns displacement; interruption and rendered pose checked |
| Crouch locomotion | Portable Quaternius idle/forward; local installed Epic idle and four directions | Local directional branch is free/high-cover only; low cover retains protective Quaternius motion |
| Crouched ADS | Armed upper-body over genuine crouch | Local profile supports four-way movement and crouched high-edge steps; portable baseline remains stationary |
| Sprint | Local rifle uses Mixamo Crouched Run with support-grip IK; pistol and portable baseline keep UAL Sprint_Loop | 500 cm/s local speed and bounded heading; no dedicated start/stop/turn variants or exact franchise reproduction |
| Aim/fire/reload/equip | Epic clips, Aim Offsets and authored montages | Reload uses an arm-only layer while crouched or behind low cover; equip and dry fire also use protective arm composition; static gun mechanics remain simple |
| Standing melee | Alternating retargeted Punch_Jab/Punch_Cross full-body montages | Inspect impact timing/contact; crouched melee disabled; no knife motion |
| Low-cover blind fire | Genuine crouch + arm-only native IK + Epic grip/fire clips | Actual hands, muzzle, head and shot geometry must pass live guards; stationary, low-cover only; source blind-fire clips remain unacquired |
| Cover | Checked smooth approach; local rifle standing wall idle/left/right; physical ADS step-out/return | Dedicated entry/exit actions, low-wall/pistol pose set, knee slide, corners and transfers remain missing or dormant; full M3 is not accepted |
| Dodge roll | Retargeted forward Roll reoriented to travel, FullBody montage and native guarded movement | Standing-clearance-checked starts from crouch/cover; no authored sideways/backward roll variants |
| Vault | Installed Epic VaultOver and Mixamo Vault Over Box candidates | Imported/inspected locally but disabled; source trajectory/contact does not yet fit attached cover traversal |
| Hit/incapacity/death | Installed candidates and source research only | No implemented life-state animation graph |
| Second-source retarget | Source/target rigs, retarget asset and seven outputs authored | Six selected clips exercised in game; sword remains dormant. Complete production transition/deformation coverage is still required |

The prior portable sandbox passed its motion, blind-fire, crouched-reload and polish checks. An earlier local graph passed 24 traversal scenarios in each of two rendered PIE sessions and a commandlet asset gate. A later normal-editor restart auto-reimported the local FBX sources and corrupted the retargeted poses, so those results do not accept the current profile. The recovered assets now pass a fresh persistence gate: 2,013 source/raw-target/root-locked-target poses across eleven clips, plus fourteen rig/target package hashes. The recovered profile passed the complete 30-scenario traversal probe twice (612 checks), including stance transitions/carry, plus movement (186), blind-fire (68), crouched-reload (224) and polish (242) checks. Portable fallback also passed 184 checks; post-normal-editor persistence revalidated all 2,013 poses and fourteen package hashes with zero errors/warnings. Final acceptance and limitations are recorded in [TEST_MATRIX](TEST_MATRIX.md).

## Further assets and acceptance

[ASSET_RESEARCH](ASSET_RESEARCH.md) records exact free coverage and optional direct-creator paid tiers. The full UAL1 catalogue has directional crouch candidates; UAL2 Source has a safety-vault candidate. Neither acquisition nor import would by itself provide all dedicated cover-shooter animation work. No paid pack was purchased. No uncertain-license GaspFix or repackaged Mixamo material was imported. The new Mixamo sources came directly from the user's signed-in official account; acquisition settings and selected/dormant coverage are recorded in [MOVEMENT_ASSET_COVERAGE](research/MOVEMENT_ASSET_COVERAGE.md).

Before enabling another motion, record source/version/license, preserve editable files, retarget to the canonical skeleton and inspect scale, root drift, foot contact, pelvis height, shoulder twist, weapon-hand contact and interruption behavior from the actual camera. Build, launch, exercise and record the result. Missing animation-dependent actions remain gated; do not substitute capsule-only crouch, relabel a climb as a vault or declare M1/M3 complete from static screenshots.

## Crouched reload composition

`ABP_WardenCrouchReload` retains the blind-fire graph and adds a protective arm-only layer for the existing Epic rifle/pistol reload montages. Crouch pelvis, spine, head and legs remain from the acquired Quaternius motion. The reload arm motion is reused without editing source tracks or pinning the hands with blind-fire IK. This is project-authored composition, not an acquired dedicated crouched-reload clip. A graph readiness flag gates protected reloads; the guarded installer preserves the previous graph and backs up the character assignment. Live pose, ammunition and interruption acceptance is recorded in TEST_MATRIX.

## Motion-polish composition

`ABP_WardenMotionPolish` preserves prior blind-fire/reload behavior and adds equip/dry-fire arm blending from each clavicle. `CrouchHandlingAlpha` contributes to `ProtectiveArmsWeight`; genuine crouch still supplies the pelvis, spine, head and legs. Existing Epic equip clips supply handling motion without blind-fire IK pinning the hands. Low-cover held ADS is suppressed during equip and resumes after completion. Interrupted equip releases the action but retains the incoming selected weapon, matching the existing equip contract.

Weapon Data Assets select `AM_Rifle/Pistol_JumpStart` and `JumpLand` on FullBody. Takeoff comes from Epic Jump_Start; landing uses Jump_RecoveryAdditive on actual ground contact. Copies under `Animation/InPlace` disable animation root motion and force the reference-pose root lock; Epic originals are preserved. CharacterMovement remains the displacement authority. Fall loops bridge the phases. Crouch, sprint, cover, dodge, firing and handling actions can interrupt recovery without locking movement.

`AM_Rifle/Pistol_DryFire` uses UpperBody. Empty trigger feedback is rate-limited to 0.8 seconds and creates no ammo/damage commit or exclusive gameplay action. Holding an empty trigger produces one presentation; reload replaces it immediately. `AM_MeleeCross` uses FullBody and alternates with the jab on accepted presses. Cross impact is requested at 28% of its duration; jab retains 38%. Both use the same authority, collision, interruption and single-hit guards. These are standing punches with the equipped weapon retained, not knife attacks.

## Local cover-movement composition

`ABP_WardenCoverMovement` and `DA_CoverMovement` live under ignored `/Game/Gunner/LicensedLocal/Mixamo/Runtime`. The committed character remains byte-for-byte portable; compatible local settings select this graph once at spawn. [LOCAL_MOVEMENT_SETUP](LOCAL_MOVEMENT_SETUP.md) documents the guarded preparation and config route.

Installed Mover source skeletons were checked against canonical Manny bone names, order, hierarchy and reference transforms. Eight independent editable copies preserve source poses; owned FK Control Rig bindings are rebound to the canonical skeleton before post-edit compilation. Five clips supply directional crouch at signed 140 cm/s; the 300 cm/s moving sources use rate scale `140 / 300`. The 390 cm vault remains dormant. Stationary crouch entry/exit clips are also selected and passed focused rendered checks, bringing the active installed set to seven crouch clips; vault remains dormant. Fresh-process validation hashes every editable bone-frame transform and timing; this is pose preservation, not a claim to compare curve tangents. It rejects retained Mover package dependencies without enabling Mover gameplay plugins.

Eleven official Mixamo clips are imported and retargeted to Manny from the 65-bone X Bot rig. Its Hips root is preserved in source. The target uses a generated ground-level root, then root locking removes travel without flattening pelvis motion. Retarget reports preserve source/target samples and package hashes. Four clips are active: `CrouchedRun`, `CoverIdleRifle`, `CoverLeftRifle` and `CoverRightRifle`. Seven entry/look/exit/turn/transfer/vault clips remain candidates. [TRANSITION_CLIP_FIT](research/TRANSITION_CLIP_FIT.md) records measured fit and concrete route/pose/weapon gates for those acquired clips.

Rifle sprint plays the hunched run at a measured reference speed of 250 cm/s, with playback scaling to local movement. Pistol sprint selects the existing UAL loop. Standing rifle wall motion blends idle and signed lateral clips at a 110 cm/s source reference, then scales up for wall travel. This branch requires actual high-cover attachment, outward facing, idle weapon state and no aim/peek; reload and ADS restore the existing armed layers. The rendered root is inset 25 cm toward the wall while the collision anchor stays 52 cm away.

A bone-space left-arm IK correction uses the evaluated `hand_r` and the weapon's rigid grip relation. It does not read the previous frame's attached mesh transform, stretch the arm or pin either hand during reload. The left-hand rotation follows the sampled grip; FullBody roll/melee montages evaluate afterward. Rendered traversal probes check grip location/rotation, head clearance, both lateral directions, reload restoration, sprint, ADS step-out/return and held aim after roll. The 30-scenario probe also covers stationary stance presentation, carry and interruption; two complete rendered sessions passed 612 checks with zero failures/skips. These checks do not certify all foot plants, camera angles or physical controllers.

A smooth root-motion-source approach uses ordinary locomotion presentation. It is not a knee-slide animation. The acquired unarmed transfer rises above the 115 cm protected height, and the installed vault's launch trajectory does not fit the attached anchor. Those actions remain disabled until actual weapon contact, full collision routes and interruption behavior pass.

### Restart persistence failure and recovered source pipeline

The earlier importer preserved FBX files under `Content/Gunner/LicensedLocal/Mixamo/SourceFiles`. Default editor folder watching subsequently sent 23 packages through Interchange reimport: X Bot, eleven source sequences and eleven retargeted sequences. Target animations inherited direct-source import metadata, so reopening the editor could replace Manny motion with incompatible source data. The affected local tree was preserved for recovery. The earlier 24-scenario pass occurred before this corruption and is not final acceptance.

The repaired importer preserves FBX sources in ignored `Saved/LicensedSources/Mixamo`, outside Content. The retargeter calls native `ClearDerivedAnimationReimportSource` to give generated Manny targets fresh generic empty `UAssetImportData`; the property is read-only to UE 5.8 Python. The native helper accepts only the local Mixamo retargeted package family. Source provenance stays in the manifest/reports; a Manny derivative must never advertise the original X Bot FBX as its direct reimport source. Fresh normal-editor persistence checks must prove that folder watching does not rewrite packages, followed by static pose/hash gates and rendered regression runs. Commandlet success alone did not catch this issue. `Tools/validate_mixamo_persistence.py` now defines a fresh-process read-only gate over all 61 reported source/raw-target/root-locked-target samples, fourteen rig/target package hashes, reference skeletons, root policy and external FBX provenance. It must run after authoring exits and again after a normal editor session. After recovery, a fresh run passed all 2,013 poses across eleven clips and fourteen package hashes. This validates saved editable poses, metadata and provenance; it does not certify compressed runtime animation or final rendered behavior.

### Stationary crouch transitions and carry

The graph builder can optionally compose the acquired installed crouch entry and exit sequences. Explicit native presentation phases follow actual CharacterMovement stance changes; sequence evaluators use controlled time at 2.5x playback. Only stationary, grounded free/high-cover changes qualify. Movement or aim intent, weapon actions, sprint, roll, falling and cover transitions cancel the pose branch instead of locking those actions. An immediate protective-low-cover bypass clears all transition weight. A quick reversal maps to the corresponding opposite-clip progress. The graph capability remains false unless both compatible clips are supplied. The final 30-scenario traversal run passed both sessions, including actual entry/exit head motion, capsule/feet/anchor preservation, settled carry and movement/ADS interruption. Stance and carry captures were inspected; this does not certify every action-order permutation or production foot contact.

The recovered local graph also carries the selected rifle/pistol arm pose throughout free/high-cover crouch, including entry/exit, through `CrouchWeaponCarryAlpha` in the existing clavicle-only mask. The authored crouch torso, pelvis and legs remain intact instead of inheriting an upright armed spine. ADS and weapon actions take precedence. Protected low cover immediately clears this carry weight and keeps its previously validated protective composition. Established rifle carry enables the evaluated support-hand correction; its transition/carry combination passed the focused rendered checks. Remaining regression status is in TEST_MATRIX.
