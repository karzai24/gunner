[ConsoleVariables]
; Compatibility workaround for reported loss of mouse look after ADS on macOS.
; Use Unreal's AppKit path for both normal and captured movement. Startup-only:
; this read-only CVar must be applied before FMacApplication creates its mouse bridge.
Slate.MacUseNewMouseControllerMovement=0
